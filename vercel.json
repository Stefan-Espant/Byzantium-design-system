{
  "buildCommand": "pnpm --filter @byzantium/core build && NUXT_IGNORE_LOCK=1 pnpm --filter playground generate && rm -rf playground/dist && cp -R playground/.output/public playground/dist",
  "outputDirectory": "playground/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null
}

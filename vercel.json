{
  "buildCommand": "pnpm --filter @byzantium/core build && pnpm --filter playground generate && mkdir -p playground/.output && mv .output/public playground/.output/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "playground/.output/public",
  "framework": null
}